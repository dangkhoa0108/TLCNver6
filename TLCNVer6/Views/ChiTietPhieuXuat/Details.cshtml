@model List<TLCNVer6.ViewModel.ChiTietPhieuXuatViewModel>

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-content">
    <div class="page-header">
        <h1>
            <i class="ace-icon fa fa fa-database"></i> <a href="#">
                Dữ liệu
            </a>

            <small>
                <i class="ace-icon fa fa-angle-double-right"></i>
                Quản lý chi tiết phiếu xuất
            </small>
        </h1>
    </div><!-- /.page-header -->
    <div class="row">
        <div class="col-xs-12">
            <!-- PAGE CONTENT BEGINS -->
            <div class="row">
                <div class="col-xs-12"></div>
                <!-- /.span -->
            </div>
            <!-- /.row -->

            <div class="row">
                <div class="col-xs-12">
                    <h3 class="header smaller lighter blue">
                        Quản lý chi tiết phiếu xuất
                    </h3>

                    <div class="clearfix">
                        <div class="pull-right tableTools-container"></div>
                    </div>
                    <div class="table-header">Quản lý chi tiết phiếu xuất</div>

                    <div>
                        <table id="dynamic-table"
                               class="table table-striped table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th>Mã phiếu xuất</th>
                                    <th>Tên mặt hàng</th>
                                    <th>Tên đơn vị</th>
                                    <th>Số lượng</th>
                                    <th>Đơn giá</th>
                                    <th>Tổng</th>
                                    <th>Thao tác</th>
                                </tr>
                            </thead>

                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.MaPX</td>
                                        <td>@item.TenMatHang</td>
                                        <td>@item.TenDonVi</td>
                                        <td>@item.SoLuong</td>
                                        <td>@item.DonGia</td>
                                        <td>@item.Sum</td>
                                        <td>
                                            <div class="hidden-sm hidden-xs action-buttons">
                                                @Html.ActionLink(" ", "Edit", new { id = item.ID }, new { @class = "ace-icon fa fa-pencil bigger-130 green" })

                                                @Html.ActionLink(" ", "Delete", new { id = item.ID }, new { @class = "ace-icon fa fa-trash-o bigger-130 red" })
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            @Html.ActionLink("Thêm mới", "Create", null, new { @class = "btn btn-xs btn-success ace-icon fa fa-plus-circle" })
            @Html.ActionLink("Trở lại", "Index", "ThongTinPhieuXuat", null, new { @class = "btn btn-xs btn-warrning ace-icon fa fa-undo" })
            <!-- PAGE CONTENT ENDS -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
</div>